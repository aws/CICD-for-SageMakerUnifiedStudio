typer[all]
PyYAML
# DataZone VPC connection support (props.vpcProperties) requires boto3/botocore
# >= 1.43.14; older SDKs silently drop the field. See pyproject.toml.
boto3>=1.43.14
botocore>=1.43.14
jsonschema
